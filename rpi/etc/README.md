# Configurations for application start
	
## thecontroler_static.conf

This file will be included into the webserver configuration when `create_proxy_config.sh` is run.
	
## 99-the-controler.conf

The webserver configuratiom create by `create_procxy_config.sh`.

It has to be linked into the `/etc/lighttpd/conf-enabled` directory.
 	